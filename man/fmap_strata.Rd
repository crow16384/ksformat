% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{fmap_strata}
\alias{fmap_strata}
\title{Build a Vector of Stratified Range Mappings}
\usage{
fmap_strata(
  stratum,
  low,
  high,
  label,
  inc_low = TRUE,
  inc_high = FALSE,
  sep = "|",
  date_format = NULL
)
}
\arguments{
\item{stratum}{Character vector of stratum identifiers.}

\item{low, high}{Range bounds. See \code{\link{fmap_ranges}}.}

\item{label}{Character vector of labels.}

\item{inc_low, inc_high}{Logical, length 1 or \code{length(low)}. See
\code{\link{fmap_ranges}}.}

\item{sep}{Separator inserted between stratum and range key. Must match
the \code{sep} subsequently passed to \code{\link{fputk}}.}

\item{date_format}{Optional strptime format string.}
}
\value{
A \code{ks_fmap} object with an attached \code{"strata_sep"}
  attribute.
}
\description{
Companion to \code{\link{fmap_ranges}} for the \code{stratified_range}
format type. Each row pairs a stratum (e.g. study arm, subject id, or a
composite key produced by \code{fputk()}) with a numeric / Date /
POSIXct range and a label. The returned \code{ks_fmap} vector carries
the chosen \code{sep} as an attribute so that
\code{\link{fnew}(type = "stratified_range")} picks it up automatically.
}
\examples{
visits <- fmap_strata(
  stratum  = c("ARM_A", "ARM_A", "ARM_B"),
  low      = c(0, 7, 0),
  high     = c(7, 14, 10),
  label    = c("Baseline", "Week 1", "Baseline")
)
fnew(visits, type = "stratified_range", range_subtype = "numeric",
     name = "visit_window")
fputk(c("ARM_A", "ARM_B"), c(3, 5), format = "visit_window")
fclear()
}
\seealso{
\code{\link{fmap_ranges}}, \code{\link{fputk}}, \code{\link{fnew}}
}
