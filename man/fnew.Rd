% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_create.R
\name{fnew}
\alias{fnew}
\title{Create a Format Definition (like SAS PROC FORMAT)}
\usage{
fnew(..., name = NULL, type = "auto", default = NULL)
}
\arguments{
\item{...}{Named arguments defining value-label mappings. Can be:
\itemize{
  \item Discrete values: \code{"M" = "Male", "F" = "Female"}
  \item Special values: \code{.missing = "Missing"}, \code{.other = "Other"}
}}

\item{name}{Character. Optional name for the format. If provided, the format
is automatically registered in the global format library.}

\item{type}{Character. Type of format: \code{"character"}, \code{"numeric"},
or \code{"auto"} (default) for auto-detection.}

\item{default}{Character. Default label for unmatched values (overrides .other)}
}
\value{
An object of class \code{"ks_format"} containing the format definition.
  The object is also stored in the format library if \code{name} is given.
}
\description{
Creates a format object that maps values to labels, similar to SAS PROC FORMAT.
Supports discrete value mapping, ranges, and special handling of missing values.
The format is automatically stored in the global format library if \code{name}
is provided.
}
\details{
Special directives:
\itemize{
  \item \code{.missing}: Label for NA, NULL, NaN values
  \item \code{.other}: Label for values not matching any rule
}
}
\examples{
# Discrete value format (auto-stored as "sex")
fnew(
  "M" = "Male",
  "F" = "Female",
  .missing = "Unknown",
  name = "sex"
)

# Apply immediately
fput(c("M", "F", NA), "sex")
fclear()
}
