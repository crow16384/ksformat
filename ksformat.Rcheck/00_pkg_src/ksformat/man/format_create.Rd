% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_create.R
\name{format_create}
\alias{format_create}
\title{Create a Format Definition (like SAS PROC FORMAT)}
\usage{
format_create(..., name = NULL, type = "auto", default = NULL)
}
\arguments{
\item{...}{Named arguments defining value-label mappings. Can be:
- Discrete values: `"M" = "Male", "F" = "Female"`
- Ranges: `c(0, 18) = "Child", c(18, 65) = "Adult"`
- Special values: `.missing = "Missing"`, `.other = "Other"`}

\item{name}{Character. Optional name for the format (like SAS format name)}

\item{type}{Character. Type of format: "character" or "numeric"}

\item{default}{Character. Default label for unmatched values (overrides .other)}
}
\value{
An object of class "ks_format" containing the format definition
}
\description{
Creates a format object that maps values to labels, similar to SAS PROC FORMAT.
Supports discrete value mapping, ranges, and special handling of missing values.
}
\details{
Special directives:
- `.missing`: Label for NA, NULL, NaN values
- `.other`: Label for values not matching any rule
- Ranges are specified as c(low, high) and are inclusive on both ends
- Use -Inf or Inf for open-ended ranges
}
\examples{
# Discrete value format
sex_fmt <- format_create(
  "M" = "Male",
  "F" = "Female",
  .missing = "Unknown"
)

# For range-based formatting, use string keys:
\dontrun{
age_fmt <- format_create(
  "0-18" = "Child",
  "18-65" = "Adult",
  "65+" = "Senior",
  .missing = "Age Unknown"
)
}
}
